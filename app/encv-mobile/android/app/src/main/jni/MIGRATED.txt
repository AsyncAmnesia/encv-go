# MIGRATED to :plugin-mpv-player module
# This directory contained MPV JNI source code (C++) and Android.mk build config.
# The native build pipeline (libmpv.so + libplayer.so) has been fully moved to
# the :plugin-mpv-player ComboLite plugin module.
#
# Status: NOT compiled — no externalNativeBuild/ndkBuild configured in app/build.gradle
# The Android.mk and *.cpp files here are kept for reference only.
