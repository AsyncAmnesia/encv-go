{
  "name": "encv-lynx-player",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "rspeedy dev",
    "build": "grep -rn 'globalThis.NativeModules\\|globalThis.lynx.getJSModule' src/ && echo 'ERROR: Forbidden Lynx API usage detected. Use NativeModules directly (typing.d.ts) and useLynxGlobalEventListener for events.' && exit 1 || rspeedy build"
  },
  "dependencies": {
    "@lynx-js/react": "^0.121.0"
  },
  "devDependencies": {
    "@lynx-js/react-rsbuild-plugin": "^0.16.2",
    "@lynx-js/rspeedy": "^0.14.4",
    "@types/react": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
